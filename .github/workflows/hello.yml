name: hello-reusable

on:
  workflow_call:
    inputs:
      who:
        type: string
        default: "world"

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, ${{ inputs.who }}!"
